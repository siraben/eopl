[ This program shows how the language supports functions
  as first-class citizens. ]
let square = proc (x) *(x,x) in
letrec map(f) = proc(l) if null?(l)
                        then emptylist
                        else cons((f car(l)), ((map f) cdr(l))) in
   ((map square) {1,2,3})
